# conversation-studio-web

会话工作台前端目录。

当前实现：
- 页面主文件位于当前目录：`conversation_studio.html`。
- 内容与 `apps/sim/user-sim-service/static/conversation_studio.html` 保持一致。
- 该目录先作为前端归档与后续独立化入口。

运行方式（当前）：
1. 启动 `apps/sim/user-sim-service`。
2. 打开 `http://localhost:8001`。
