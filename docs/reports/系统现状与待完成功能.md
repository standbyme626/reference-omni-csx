# Platform-Sim 系统现状与待完成文档

## 一、项目目标

**核心定位**: 纯模拟系统，不处理任何真实数据，专门模拟"官方平台API返回"，供中台系统开发测试使用。

**最终效果**:
```
┌─────────────────────────────────────────────────────────────┐
│  LLM (扮演用户)           official-sim-server (扮演官方)    │
│       │                           │                         │
│  "我要退款"              返回京东官方退款API格式            │
│  "查物流"                返回官方物流查询格式               │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、参考设计文档

**重要参考**: `docs/archive/notes/1.md` 中的 User Simulator Agent 设计

核心要点:
1. **三层架构**: World Engine / User Agent / Platform Layer
2. **两阶段输出**: 结构化意图 + 用户话术
3. **用户视角工具**: 只暴露用户能感知的信息
4. **Evaluator**: 验证消息是否符合真实用户

---

## 三、架构设计 (三层架构)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           完整架构图                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1. World / Scenario Engine (真相层)                                 │   │
│  │                                                                     │   │
│  │  维护:                                                               │   │
│  │  - user / order / shipment / refund                                 │   │
│  │  - conversation / emotion                                            │   │
│  │  - current_step / platform_state                                    │   │
│  │                                                                     │   │
│  │  特点: 不靠LLM决定真相，靠状态机 + fixtures + rules                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ↓                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  2. User Simulator Agent (用户模拟)                                  │   │
│  │                                                                     │   │
│  │  职责:                                                               │   │
│  │  - 读当前用户可见事实                                                │   │
│  │  - 判断"真实用户现在最可能问什么"                                     │   │
│  │  - 生成自然语言消息                                                  │   │
│  │  - 决定要不要追问、升级情绪、要求人工、提退款                         │   │
│  │                                                                     │   │
│  │  工具: (见下方工具定义)                                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ↓                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  3. Platform Behavior Layer (官方仿真)                               │   │
│  │                                                                     │   │
│  │  职责: 把状态引擎里的事实变成官方API返回                              │   │
│  │  - 官方订单查询返回                                                  │   │
│  │  - 官方物流返回                                                     │   │
│  │  - 官方退款返回                                                     │   │
│  │  - webhook / callback / push payload                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、三层职责边界

| 层级 | 谁负责 | 做什么 | 不做什么 |
|-----|-------|-------|---------|
| **World Engine** | 状态机/fixtures | 真相(订单状态/物流/退款) | 不生成用户消息 |
| **User Agent** | LLM | 生成用户消息 | 不决定世界真相 |
| **Platform Layer** | fixtures | 官方API格式 | 不生成对话 |

---

## 五、User Agent 设计

### 5.1 两阶段输出

```
┌─────────────────────────────────────────────────────────────────┐
│  阶段1: 结构化"用户意图决策"                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ {                                                       │    │
│  │   "selected_user_id": "u_001",                         │    │
│  │   "selected_order_id": "JD_ORDER_001",                │    │
│  │   "intent": "ask_refund_progress",                    │    │
│  │   "emotion": "impatient",                             │    │
│  │   "tool_calls_used": [...],                           │    │
│  │   "reason": "订单已付款且退款中，用户最可能追问退款进度"   │    │
│  │ }                                                       │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              ↓                                      │
│  阶段2: 用户话术                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ {                                                       │    │
│  │   "user_message": "我的退款怎么还没处理好？...",         │    │
│  │   "intent": "ask_refund_progress",                     │    │
│  │   "emotion": "impatient"                               │    │
│  │ }                                                       │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 最终输出结构 (Structured Outputs)

```json
{
  "selected_user_id": "string",
  "selected_order_id": "string|null",
  "intent": "ask_order_status|ask_shipment|ask_refund|complain|escalate_to_human|product_question",
  "emotion": "calm|impatient|angry",
  "tool_calls_used": [
    {
      "name": "string",
      "arguments": {}
    }
  ],
  "user_message": "string",
  "reason": "string"
}
```

---

## 六、工具定义 (Tools Schema)

### 6.1 用户视角工具 (给LLM用)

| 工具名 | 参数 | 返回 | 说明 |
|-------|-----|-----|------|
| `list_user_orders` | user_id | 订单列表 | 用户可看到的订单 |
| `get_order_summary` | order_id | 订单摘要 | 用户视角的订单信息 |
| `get_shipment_summary` | order_id | 物流摘要 | 物流状态概览 |
| `get_refund_summary` | order_id | 退款摘要 | 退款进度概览 |
| `get_recent_messages` | user_id, conversation_id | 消息列表 | 最近对话 |
| `get_user_profile` | user_id | 用户信息 | 基本资料 |

### 6.2 系统内部工具 (不给LLM用)

| 工具名 | 说明 |
|-------|------|
| `advance_order_state` | 推进订单状态 |
| `emit_platform_push` | 发送平台推送 |
| `inject_error` | 注入错误 |
| `write_artifact` | 写入artifact |
| `sync_to_unified_layer` | 同步到unified层 |

### 6.3 场景辅助工具

| 工具名 | 参数 | 返回 | 说明 |
|-------|-----|-----|------|
| `list_candidate_scenarios` | user_id | 场景列表 | 可选场景 |
| `emit_user_message` | conversation_id, message, metadata | - | 发消息 |

### 6.4 工具详细Schema

```json
{
  "tools": [
    {
      "name": "list_user_orders",
      "description": "获取用户所有订单列表",
      "parameters": {
        "type": "object",
        "properties": {
          "user_id": {"type": "string", "description": "用户ID"}
        },
        "required": ["user_id"]
      }
    },
    {
      "name": "get_order_summary",
      "description": "获取订单摘要(用户视角)",
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {"type": "string", "description": "订单ID"}
        },
        "required": ["order_id"]
      }
    },
    {
      "name": "get_shipment_summary",
      "description": "获取物流摘要",
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {"type": "string", "description": "订单ID"}
        },
        "required": ["order_id"]
      }
    },
    {
      "name": "get_refund_summary",
      "description": "获取退款摘要",
      "parameters": {
        "type": "object",
        "properties": {
          "order_id": {"type": "string", "description": "订单ID"}
        },
        "required": ["order_id"]
      }
    },
    {
      "name": "emit_user_message",
      "description": "发送用户消息(落库/推送中台)",
      "parameters": {
        "type": "object",
        "properties": {
          "conversation_id": {"type": "string"},
          "message": {"type": "string"},
          "metadata": {"type": "object"}
        },
        "required": ["conversation_id", "message"]
      }
    }
  ]
}
```

---

## 七、Prompt 约束

### 7.1 核心约束

```
1. 只能基于工具结果说话
   "你不能编造订单状态、物流状态、退款状态。
    你只能依据工具返回结果生成用户消息。"

2. 只模拟用户，不模拟客服
   "你扮演用户，不要替客服解释，不要输出客服建议，
    不要调用系统内部管理动作。"

3. 先判断用户最关心的问题，再决定是否调用工具
   "先判断用户最关心的问题，再决定是否调用工具"

4. 输出先结构化，再生成文本
   "先产出结构化意图，再生成用户话术"
```

### 7.2 Prompt 模板

```
你是用户行为模拟器。你的任务是根据用户信息生成真实的用户消息。

## 用户信息
- 用户ID: {user_id}
- 用户名: {user_name}
- 平台: {platform}

## 可用工具
{tools_schema}

## 核心约束
1. 你不能编造订单状态、物流状态、退款状态
2. 你只能依据工具返回结果生成用户消息
3. 你扮演用户，不要替客服解释
4. 先判断用户最关心的问题，再决定是否调用工具

## 你的任务
1. 选择一个用户和该用户的订单
2. 根据订单状态，决定用户可能有什么问题
3. 调用合适的工具获取数据
4. 基于数据生成用户消息

## 用户消息要求
- 口语化，像真实用户
- 可以包含订单号等信息
- 不要太长，1-3句话

## 输出格式 (两阶段)
阶段1 - 结构化决策:
{
  "selected_user_id": "...",
  "selected_order_id": "...",
  "intent": "...",
  "emotion": "...",
  "tool_calls_used": [...],
  "reason": "..."
}

阶段2 - 用户话术:
{
  "user_message": "..."
}
```

---

## 八、Evaluator (消息校验)

### 8.1 校验规则

| 校验项 | 说明 |
|-------|------|
| 引用不存在的order_id | 错误 |
| 说与refund_status不一致的话 | 错误 |
| 用了平台看不到的内部字段 | 错误 |
| 长度超标 | 错误 |
| 没有情绪/意图标签 | 错误 |

### 8.2 校验实现

```python
def validate_message(message: dict, tool_results: list) -> ValidationResult:
    errors = []

    # 检查order_id是否存在
    if message.get("selected_order_id"):
        if not order_exists(message["selected_order_id"]):
            errors.append("引用了不存在的order_id")

    # 检查与工具结果是否矛盾
    if message.get("intent") == "ask_refund":
        if not has_refund_record(message["selected_order_id"]):
            errors.append("用户说退款但工具显示无退款记录")

    # 检查是否用了内部字段
    internal_fields = ["advance_order_state", "emit_platform_push"]
    if any(f in str(message) for f in internal_fields):
        errors.append("使用了系统内部字段")

    return ValidationResult(passed=len(errors) == 0, errors=errors)
```

---

## 九、LangGraph 流程

### 9.1 节点设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           LangGraph 执行流                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  select_user → select_order → decide_intent                               │
│       ↓            ↓              ↓                                        │
│  选择用户      选择订单      判断用户意图                                     │
│       ↓            ↓              ↓                                        │
│  call_tools ←─────────────────→ │                                          │
│       ↓                                                                   │
│  render_message                                                          │
│       ↓                                                                   │
│  validate_message                                                         │
│       ↓                                                                   │
│  emit_message (调用emit_user_message工具)                                  │
│       ↓                                                                   │
│  record_artifact                                                         │
│       ↓                                                                   │
│  end                                                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 9.2 State 定义

```python
class UserSimulatorState(TypedDict):
    run_id: str
    user_id: Optional[str]
    platform: str
    order_id: Optional[str]
    refund_id: Optional[str]
    conversation_id: Optional[str]

    # 决策
    intent: Optional[str]
    emotion: Optional[str]
    tool_results: List[dict]

    # 输出
    user_message: Optional[str]
    validation_result: Optional[dict]

    # 审计
    steps: List[dict]
    artifacts: List[dict]
```

---

## 十、API 草图

### 10.1 User Simulator API

```bash
# 生成用户消息
POST /user-simulator/generate

请求:
{
  "platform": "jd",
  "user_id": "u_001",           # 可选，不传则随机选用户
  "conversation_id": "conv_001",
  "scenario_hint": "refund"      # 可选，引导场景
}

响应:
{
  "code": "0",
  "message": "success",
  "data": {
    "selected_user_id": "u_001",
    "selected_order_id": "JD_ORDER_001",
    "intent": "ask_refund",
    "emotion": "impatient",
    "user_message": "我的退款怎么还没处理好？订单号 JD_ORDER_001，已经好几天了。",
    "tool_calls_used": [
      {"name": "get_order_summary", "arguments": {"order_id": "JD_ORDER_001"}},
      {"name": "get_refund_summary", "arguments": {"order_id": "JD_ORDER_001"}}
    ],
    "reason": "用户有退款记录且等待中，最可能追问进度"
  }
}
```

### 10.2 Platform Query API (给工具用)

```bash
# 查询订单
GET /official-sim/{platform}/orders/{order_id}

响应:
{
  "code": "0",
  "data": {
    "order_summary": {
      "order_id": "JD_ORDER_001",
      "platform": "jd",
      "order_status": "paid",
      "amount": "9999.00",
      "created_at": "2026-03-28T10:00:00+08:00",
      "items": ["iPhone 15 Pro Max x1"]
    },
    "official_response": { ...京东官方格式... }
  }
}

# 查询退款
GET /official-sim/{platform}/refunds/{order_id}

响应:
{
  "code": "0",
  "data": {
    "refund_summary": {
      "order_id": "JD_ORDER_001",
      "refund_status": "refunding",
      "refund_amount": "9999.00",
      "apply_time": "2026-03-29T10:00:00+08:00"
    },
    "official_response": { ...京东退款官方格式... }
  }
}
```

---

## 十一、FIXTURES 数据结构

### 11.1 用户数据结构

```json
{
  "user_id": "u_001",
  "platform": "jd",
  "name": "张三",
  "phone": "138****1234",
  "created_at": "2026-01-01T00:00:00+08:00",
  "orders": [
    {
      "order_id": "JD_ORDER_001",
      "status": "paid",
      "amount": "9999.00",
      "items": [
        {
          "name": "iPhone 15 Pro Max 256GB",
          "quantity": 1,
          "price": "9999.00"
        }
      ],
      "receiver": {
        "name": "张三",
        "phone": "138****1234",
        "address": "北京市海淀区中关村xxx"
      },
      "shipment": {
        "status": "in_transit",
        "company": "京东物流",
        "tracking_no": "JD1234567890",
        "nodes": [
          {"node": "已发货", "time": "2026-03-29T10:00:00+08:00"},
          {"node": "运输中", "time": "2026-03-30T08:00:00+08:00"}
        ]
      },
      "refund": {
        "status": "refunding",
        "amount": "9999.00",
        "reason": "商品损坏",
        "apply_time": "2026-03-29T12:00:00+08:00"
      },
      "created_at": "2026-03-28T10:00:00+08:00",
      "paid_at": "2026-03-28T10:05:00+08:00"
    }
  ]
}
```

### 11.2 平台用户数量要求

| 平台 | 用户数 | 说明 |
|-----|-------|------|
| taobao | 3+ | 模拟真实用户多样性 |
| douyin_shop | 3+ | |
| jd | 3+ | |
| xhs | 3+ | |
| kuaishou | 3+ | |
| wecom_kf | 3+ | |

---

## 十二、实现顺序

### P0 (本周)

```
Step 1: Fixtures 数据
├── 每个平台创建3个用户fixture
└── 包含完整订单/物流/退款数据

Step 2: User Agent 基础结构
├── ai-orchestrator/nodes/user_simulator.py
├── 定义 State
├── 实现 LangGraph 节点
└── 实现基础工具函数

Step 3: 工具 API
├── GET /official-sim/{platform}/orders/{order_id}
├── GET /official-sim/{platform}/refunds/{order_id}
└── GET /official-sim/{platform}/shipments/{order_id}
```

### P1 (下周)

```
Step 4: Prompt 优化
├── 两阶段输出
├── 核心约束
└── Structured Outputs

Step 5: Evaluator
├── 规则校验
└── 消息验证

Step 6: 完整流程串联
└── 测试用户消息生成 → 平台响应 → 中台处理
```

---

## 十三、常见错误 (避免)

| 错误 | 后果 | 正确做法 |
|-----|------|---------|
| 让Agent决定"世界真相" | 不再是官方仿真 | 真相来自World Engine |
| 直接暴露平台底层原始字段 | 生成的话术不像人话 | 先做summary tool |
| 一步输出最终文本 | 不利于测试审计 | 两阶段输出 |
| 工具不给LLM暴露 | Agent无法获取数据 | 分用户工具/系统工具 |

---

## 十五、数据集选择记录

### 选定的数据集

| 数据集 | 来源 | 用途 | 许可 | 状态 |
|-------|------|------|------|------|
| **JDDC Corpus** | 京东AI发布 | 主问题池，查订单/退款/物流 | Apache 2.0 | 待下载 |
| E-commerce Dialogue Corpus | Google Drive | 用户怎么问 | 待复核 | 备选 |
| JDDC 2.1 | GitHub (需申请) | 多模态图片举证 | 需申请 | P1 |
| 天猫对话测试集 | ModelScope | smoke test/demo | 待复核 | P2 |

### 数据集使用方式

```
数据集 → 训练/参考 → LLM学会真实用户问法
                         ↓
Fixtures → 提供事实 → LLM调用工具查订单状态
                         ↓
LLM生成: "我的退款怎么还没好？"
```

### 下载入口

```
1. JDDC Corpus
   - 来源: 京东AI
   - 地址: https://jddc.jd.com (需注册)
   - 用途: 主问题池

2. E-commerce Dialogue Corpus
   - 来源: Google Drive
   - 地址: GitHub README有链接
   - 用途: 用户问法模板

3. JDDC 2.1
   - 来源: hrlinlp/jddc2.1
   - 地址: GitHub (需填表申请)
   - 用途: 多模态场景
```

### TODO 清单

#### 紧急

- [x] 创建 `fixtures/{platform}/users/` 目录结构
- [x] 为6个平台各创建3个用户fixture
- [x] 新增 `GET /official-sim/query/orders/{order_id}` API
- [x] 新增 `GET /official-sim/query/orders/{order_id}/refund` API
- [x] 新增 `GET /official-sim/query/orders/{order_id}/shipment` API
- [x] 下载淘宝客服数据集 (ECD) - 100万 utterances, 54MB
- [x] 提取ECD数据中有价值的用户问题模板
- [x] 整合ECD模板到User Simulator的system prompt (2946条)
- [x] 实现 Reply Adapter (OfficialSim + Stub)
- [x] 实现多轮上下文管理器 (ConversationContext)
- [x] 实现双循环 LangGraph 状态机 (ConversationStudioGraph)
- [x] 实现 conversation-studio API 路由
- [x] 实现 Conversation Studio 前端界面
- [ ] 下载 JDDC Corpus 数据集

#### 重要

- [x] 创建 `ai-orchestrator/nodes/user_simulator.py`
- [x] 实现 `list_user_orders` 工具
- [x] 实现 `get_order_summary` 工具
- [x] 实现 `emit_user_message` 工具
- [x] 实现 Evaluator 规则校验
- [x] 实现 Conversation Studio 多轮对话工作台
- [ ] 整合 JDDC 数据集到 Prompt

#### 优化

- [ ] Structured Outputs 集成
- [x] 整合ECD模板到User Simulator (2946条模板)
- [x] 多轮对话状态机实现 (LangGraph) - 双循环状态机

---

### 已实现的 Conversation Studio

#### 核心组件

| 组件 | 文件路径 | 说明 |
|-----|---------|------|
| Reply Adapter | `nodes/reply/` | OfficialSim + Stub 双模式 |
| ConversationContext | `nodes/conversation/context.py` | 多轮上下文管理 |
| ConversationStudioGraph | `nodes/conversation_studio.py` | 双循环状态机 |
| API Routes | `api/routes/conversation_studio.py` | 5个接口 |
| 前端界面 | `static/conversation_studio.html` | 三栏布局 |

#### API 接口

```
POST /conversation-studio/runs          # 创建对话
POST /conversation-studio/runs/{id}/next # 下一轮
GET  /conversation-studio/runs/{id}      # 获取摘要
GET  /conversation-studio/runs/{id}/debug # 调试信息
GET  /conversation-studio/runs/{id}/messages # 消息历史
```

#### 前端布局

```
┌─────────────┬────────────────────┬─────────────┐
│  场景控制   │    对话窗口        │  调试面板   │
│  - 平台     │  用户消息          │  事实摘要   │
│  - 情绪     │  客服回复          │  工具调用   │
│  - 类型     │  时间戳/轮次       │  API响应    │
│  - 轮次     │                    │             │
└─────────────┴────────────────────┴─────────────┘
```

---

### 多轮对话状态机设计（待实现）

#### 状态定义
```
States:
- IDLE: 初始化
- SELECT_USER: 选择用户和订单
- THINK: LLM决策下一步做什么
- CALL_TOOL: 调用工具查询数据
- GENERATE_MSG: 生成用户消息
- CALL_API: 调用official-sim-server获取客服回复
- UPDATE_CONTEXT: 更新对话上下文
- DECIDE_CONTINUE: 判断是否继续
- END: 结束
```

#### 状态流转
```
START → IDLE → SELECT_USER → THINK → CALL_TOOL → GENERATE_MSG
                                              ↓
                                       CALL_API → UPDATE_CONTEXT
                                              ↓
                                       DECIDE_CONTINUE
                                         ↓           ↓
                                      [继续]      [结束]
                                         ↓           ↓
                                       THINK        END
```

#### 节点设计
```python
nodes = {
    "select_user": 选择用户和订单,
    "think": LLM决策下一步(查询/生成消息/结束),
    "call_tool": 调用FixtureLoader工具,
    "generate_message": 生成用户消息,
    "call_official_api": 调用official-sim-server,
    "update_context": 更新对话历史和状态,
    "decide_continue": 判断是否继续对话,
}
```

#### 待讨论问题
1. 客服回复来源: official-sim-server API vs LLM生成
2. 订单状态更新机制
3. 最大轮次限制 (建议5-10轮)

---

## 十六、代码改动记录

### 16.1 FixtureLoader 扩展

**文件**: `providers/utils/fixture_loader.py`

**新增方法**:
- `list_users(platform)` - 列出平台所有用户ID
- `load_user(platform, user_id)` - 加载用户数据
- `get_user_order(platform, user_id, order_id)` - 获取用户指定订单
- `get_user_orders(platform, user_id)` - 获取用户所有订单
- `get_user_by_order(platform, order_id)` - 通过订单ID查找用户

### 16.2 Query API

**文件**: `apps/official-sim-server/app/api/routes/query.py` (新建)

**路由**:
```
GET /official-sim/query/users?platform=jd
GET /official-sim/query/users/{user_id}?platform=jd
GET /official-sim/query/users/{user_id}/orders?platform=jd
GET /official-sim/query/orders/{order_id}?platform=jd
GET /official-sim/query/orders/{order_id}/shipment?platform=jd
GET /official-sim/query/orders/{order_id}/refund?platform=jd
```

### 16.3 用户Fixtures

**创建的文件** (共18个):

| 平台 | 用户ID | 文件路径 |
|-----|--------|---------|
| jd | jd_user_001~003 | `fixtures/jd/users/` |
| taobao | taobao_user_001~003 | `fixtures/taobao/users/` |
| douyin_shop | douyin_user_001~003 | `fixtures/douyin_shop/users/` |
| xhs | xhs_user_001~003 | `fixtures/xhs/users/` |
| kuaishou | kuaishou_user_001~003 | `fixtures/kuaishou/users/` |
| wecom_kf | wecom_user_001~003 | `fixtures/wecom_kf/users/` |

### 16.4 User Simulator Agent

**文件**: `apps/ai-orchestrator/nodes/user_simulator.py` (新建)

**核心类**:
- `UserSimulator` - 主类，负责生成用户消息
- `UserSimulatorState` - 状态管理
- `UserSimulatorOutput` - 输出结构
- `UserMessageDecision` - 决策结构
- `IntentType` - 意图枚举
- `EmotionType` - 情绪枚举

**核心方法**:
- `generate(platform, user_id, conversation_id)` - 生成用户消息
- `list_user_orders(user_id, platform)` - 获取用户订单
- `get_order_summary(order_id, platform)` - 获取订单摘要
- `get_shipment_summary(order_id, platform)` - 获取物流摘要
- `get_refund_summary(order_id, platform)` - 获取退款摘要
- `emit_user_message(conversation_id, message, metadata)` - 发送消息
- `get_available_tools()` - 获取可用工具列表
- `build_system_prompt(platform)` - 构建系统提示词

**测试结果**:
```
User: jd_user_003
Order: JD_ORDER_007
Intent: ask_shipment
Emotion: impatient
User Message: 我的订单JD_ORDER_007已经付款好几天了，怎么还没发货啊？
```

### 16.5 Evaluator 规则校验

**文件**: `apps/ai-orchestrator/nodes/evaluator.py` (新建)

**核心类**:
- `Evaluator` - 主类，负责消息规则校验
- `ValidationError` - 错误结构
- `ValidationResult` - 校验结果结构

**校验规则**:
1. `_check_order_exists` - 检查引用的订单是否存在
2. `_check_refund_consistency` - 检查退款意图与订单退款状态一致性
3. `_check_internal_fields` - 检查消息是否包含系统内部字段
4. `_check_message_length` - 检查消息长度（警告，超过200字）
5. `_check_intent_emotion_consistency` - 检查意图与情绪一致性

**测试结果**:
```
Test 1 (valid message): Passed=True, Errors=0
Test 2 (non-existent order): Passed=False, Errors=['引用了不存在的订单: INVALID_ORDER']
Test 3 (refund inconsistency): Passed=False, Errors=['用户说要退款但订单 JD_ORDER_001 没有退款记录']
Test 4 (refund consistency): Passed=True, Errors=0
```

### 16.6 LangGraph 工作流

**文件**: `apps/ai-orchestrator/nodes/user_simulator_graph.py` (新建)

**核心类**:
- `UserSimulatorGraph` - LangGraph工作流主类
- `UserSimulatorGraphState` - 状态定义

**工作流节点**:
1. `select_user` - 选择用户
2. `select_order` - 选择订单
3. `decide_intent` - 决定意图和情绪
4. `call_tools` - 调用工具获取数据
5. `render_message` - 渲染用户消息
6. `validate_message` - 校验消息
7. `emit_message` - 发送消息

**测试结果**:
```
Status: completed
User: jd_user_002
Order: JD_ORDER_005
Intent: ask_order_status
Emotion: calm
User Message: 我的订单JD_ORDER_005是不是已经收到了呀，显示已签收了？
Validation: Passed=True
```

---

# 十二、当前进度与问题 (2026-03-30 更新)

## 12.1 已完成功能

### ✅ Fixture 场景全覆盖 (77个)

| 平台 | success | edge_case | error_case | 总计 | 覆盖率 |
|------|---------|-----------|------------|------|--------|
| 淘宝 | 15 | 2 | 4 | 21 | 100% |
| 京东 | 13 | 1 | 1 | 15 | 100% |
| 抖音小店 | 12 | 0 | 2 | 14 | 100% |
| 小红书 | 10 | 1 | 1 | 12 | 100% |
| 快手 | 8 | 1 | 1 | 10 | 100% |
| 企微客服 | 3 | 0 | 2 | 5 | 100% |
| **总计** | **61** | **5** | **11** | **77** | **100%** |

### ✅ 核心模块实现

1. **UserSimulator** (`apps/ai-orchestrator/nodes/user_simulator.py`)
   - 用户选择、订单选择
   - 意图决策、情绪判断
   - 用户消息生成
   - Fallback 模式（LLM 不可用时）

2. **UserSimulatorGraph** (`apps/ai-orchestrator/nodes/user_simulator_graph.py`)
   - LangGraph 状态机流程
   - 7 个节点: select_user → select_order → decide_intent → call_tools → render_message → validate_message → emit_message

3. **ConversationStudio** (`apps/ai-orchestrator/nodes/conversation_studio.py`)
   - 对话流程管理
   - 情绪升级机制
   - 转人工判断

4. **Official-Sim Server** (`apps/official-sim-server/`)
   - Fixture 加载器
   - Query API (订单/物流/退款查询)
   - Run 管理 API

5. **前端页面**
   - Conversation Studio (`apps/ai-orchestrator/static/conversation_studio.html`)
   - 平台选择、情绪选择、最大轮次设置
   - 实时对话展示

### ✅ API 接口

| 接口 | 路径 | 状态 |
|------|------|------|
| 创建对话 | POST /conversation-studio/runs | ✅ |
| 下一轮 | POST /conversation-studio/runs/{run_id}/next | ✅ |
| 查询订单 | GET /official-sim/query/orders/{order_id} | ✅ |
| 查询物流 | GET /official-sim/query/orders/{order_id}/shipment | ✅ |
| 查询退款 | GET /official-sim/query/orders/{order_id}/refund | ✅ |

---

## 12.2 待完成功能

### 🔲 P0 优先级

1. **LLM 服务网络问题**
   - 当前无法访问阿里云 DashScope API
   - 需要解决网络代理/VPN 问题
   - 临时方案: 已实现 Fallback 模式

2. **Reply Adapter 完善**
   - Stub 模式 (本地模拟回复) - 已实现
   - Official-Sim 模式 (官方API模拟) - 待完善

3. **Error Injection 完善**
   - 当前已有基本错误注入
   - 需要完善更多错误场景

4. **Evaluation Report**
   - 生成对话评估报告
   - 统计对话质量

### 🔲 P1 优先级

1. **多轮对话连贯性**
   - 上下文记忆
   - 追问机制

2. **场景编排**
   - 预定义场景模板
   - 场景回放

3. **与企业微信/IM 对接**
   - 真实发送消息
   - Webhook 回调

---

## 12.3 当前问题

### ❌ 核心问题: LLM API 无法访问

**现象**:
```
SSLError: HTTPSConnectionPool(host='dashscope.aliyuncs.com', port=443)
```

**原因分析**:
- 当前环境网络受限，无法访问外网
- `ping dashscope.aliyuncs.com` 返回 `198.18.0.56` (异常 IP 段)
- `curl https://www.baidu.com` 返回网络不通

**影响**:
- 用户消息生成依赖 LLM，当前只能使用 Fallback 模式
- Fallback 模式使用预设模板，消息较为单调

**解决方案**:
1. 检查/配置网络代理
2. 使用 Tailscale VPN (当前机器有 tailscale0 接口)
3. 部署到可访问外网的环境

### ⚠️ 次要问题

1. **Fixture 数据有限**
   - 当前用户/订单数据较少
   - 需要扩充更多场景数据

2. **Official-Sim API 覆盖不完整**
   - 部分平台 API 未完全实现
   - 需要补充更多 API 接口

3. **前端交互体验**
   - 当前为单页 HTML
   - 可考虑 React/Vue 重构

---

## 12.4 系统运行链路

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           完整运行链路                                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 前端发起请求                                                          │
│     POST /conversation-studio/runs                                       │
│     {platform: "taobao", emotion: "calm", max_turns: 3}                 │
│                         ↓                                                 │
│                                                                         │
│  2. ConversationStudioGraph.create_run()                                │
│     - 创建 ConversationContext                                          │
│     - 生成 run_id, conversation_id                                       │
│                         ↓                                                 │
│                                                                         │
│  3. 前端调用 next_turn                                                   │
│     POST /conversation-studio/runs/{run_id}/next                         │
│                         ↓                                                 │
│                                                                         │
│  4. UserSimulator.generate()                                            │
│     - 选择用户、订单                                                      │
│     - [如 LLM 可用] 调用 LLM 生成用户消息                                 │
│     - [如 LLM 不可用] 使用 Fallback 模板生成                              │
│                         ↓                                                 │
│                                                                         │
│  5. ReplyAdapter.get_reply()                                            │
│     - Stub 模式: 返回预设回复                                             │
│     - Official-Sim 模式: 返回官方 API 格式数据                            │
│                         ↓                                                 │
│                                                                         │
│  6. 返回对话结果                                                          │
│     {user_message, reply_message, intent, emotion, ...}                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 12.5 验证结果

### API 测试 (使用 Fallback 模式)

```bash
# 创建对话
$ curl -X POST http://localhost:8000/conversation-studio/runs \
  -H "Content-Type: application/json" \
  -d '{"platform": "taobao", "emotion": "calm", "max_turns": 1}'
  
# 返回
{"run_id":"cs_run_73c74624","conversation_id":"conv_fdcb70e6","platform":"taobao","status":"created"}

# 下一轮
$ curl -X POST http://localhost:8000/conversation-studio/runs/cs_run_73c74624/next \
  -H "Content-Type: application/json" -d '{}'

# 返回 (Fallback 模式)
{
  "user_message": "帮我查一下订单TB_ORDER_007",
  "reply_message": "订单已确认，我们会尽快为您发货。",
  "reply_source": "stub",
  "intent": "ask_order_status",
  "emotion": "calm"
}
```

### Fixture 加载验证

```bash
$ python -c "from providers.utils.fixture_loader import FixtureLoader; \
  print(FixtureLoader.list_fixtures('taobao', 'success'))"

# 返回
['refund_closed', 'refund_no_refund', 'refund_refunded', ...] (15个)
```

---

## 12.6 下一步计划

1. **解决 LLM 网络问题** (P0)
   - 检查代理配置
   - 确认 Tailscale VPN 状态
   - 或部署到可访问外网的环境

2. **完善 Reply Adapter** (P0)
   - 实现完整的 Official-Sim 模式
   - 支持更多回复模板

3. **扩充 Fixture 数据** (P1)
   - 添加更多用户/订单数据
   - 完善各平台 API 字段

4. **添加测试** (P1)
   - 单元测试
   - 集成测试
